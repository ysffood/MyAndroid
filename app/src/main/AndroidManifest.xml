<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yk.demo.myandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <application
        android:name=".DefaultApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppBaseTheme">
        <activity
            android:name=".test.TestHttpRequestActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppStartLoadTranslucent" />
        <activity
            android:name=".test.TestBasicDownActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".test.TestBackgroundDownActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".test.UpFileActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".test.TestFrescoActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".test.TestDaoActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".test.CombinedListviewActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".test.RecycleActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme" />
        <activity android:name=".test.HomeFragmentActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>